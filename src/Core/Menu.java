package Core;

public class Menu {
    public enum ClientMenu{
        EXIT,
        FIND_ALL_AIRPORT,
        FIND_BY_ID,
        INSERT_AIRPORT,
        FILTER_AIRPORT,
        FIND_ALL_ARRIVAL,
        DELETE_BY_ID,
        VIEW_ARRIVAL_SORTED,
        ARRIVING_FROM,
         None
    }
}
